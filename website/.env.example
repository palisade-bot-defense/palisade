# Private business contact or scheduling URL used by the public site.
# Example: mailto:security@example.com or https://cal.example.com/palisade
VITE_CONTACT_URL=

# Reviewed canonical HTTPS origin. Without it, builds emit noindex robots
# metadata and no sitemap so an unfinished deployment cannot become canonical.
VITE_PUBLIC_SITE_URL=

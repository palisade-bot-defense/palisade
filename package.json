{
  "name": "palisade-workspace",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.24.0"
  },
  "scripts": {
    "build": "pnpm --filter @palisade-human-trust/sensor build && pnpm --filter @palisade-human-trust/dashboard build && pnpm --filter @palisade-human-trust/website build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}

# Generate every value independently with:
# openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
PALISADE_HMAC_KEY=replace-with-base64url-secret
PALISADE_API_KEY=replace-me
PALISADE_ADMIN_KEY=replace-me-too
